server:
  port: 8080

spring:
  application:
    name: smart-energy-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_energy
    username: energy_user
    password: energy_password
    driver-class-name: org.postgresql.Driver
  profiles:
    active: dev
  sql:
    init:
      # 🆕 合并 workorder-backend: 改用 ${SQL_INIT_MODE:always} 兼容 prod
      mode: ${SQL_INIT_MODE:always}
      continue-on-error: false
      encoding: UTF-8
  jackson:
    # 🆕 合并 workorder-backend: 保留全局 non_null（具体 DTO 上 @JsonInclude(ALWAYS) 显式保留 null）
    default-property-inclusion: non_null
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
      # 生产环境必须配置 password 和 SSL：
      # password: ${REDIS_PASSWORD}
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
          min-idle: 2
          max-wait: 2000ms

# Epic 6-3: Python 能耗预测微服务
prediction:
  service:
    base-url: http://localhost:8000
    timeout-ms: 8000       # MC Dropout×10 + 并发预热下单次推理可能 >4s，放宽避免超时降级
    lookback: 96          # 取最近 96 条读数作为预测输入窗口

# MyBatis-Plus（合并 workorder-backend 的 type-aliases-package）
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.smartenergy.backend.entity,com.smartenergy.workorder.entity
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# SpringDoc
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  packages-to-scan: com.smartenergy.backend.controller,com.smartenergy.workorder.controller

logging:
  level:
    com.smartenergy: INFO
    org.springframework.jdbc.core.JdbcTemplate: WARN
